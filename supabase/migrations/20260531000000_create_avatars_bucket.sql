-- Public storage bucket for ally and user profile photos.
-- 5 MB limit matches the UI hint ("UP TO 5MB"). Public = URLs are shareable.
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES (
  'avatars',
  'avatars',
  true,
  5242880,
  ARRAY['image/jpeg', 'image/png', 'image/webp']
)
ON CONFLICT (id) DO NOTHING;
